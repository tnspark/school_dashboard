# TN Spark Dashboard - Deployment Dependencies for Streamlit Cloud

# Use a recent Streamlit version that supports the new DataFrame API
# and the `width='stretch'` parameter we adopted in the app.
streamlit>=1.50.0

# Core data libraries
pandas>=2.1
numpy>=1.25

# Interactive charts
plotly>=5.18

# Advanced tables and interactivity
streamlit-aggrid>=0.3.5
streamlit-plotly-events>=0.0.6

# Exports (images and PDF)
kaleido>=0.2.1
reportlab>=4.0.9
openpyxl>=3.1

# Optional: Arrow is often pulled in by Streamlit; include for stability
# Uncomment if needed
# pyarrow>=14.0